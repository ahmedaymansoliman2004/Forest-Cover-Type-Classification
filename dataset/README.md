# Dataset

This project uses the UCI Covertype dataset.

Due to GitHub file size limits, `covtype.csv` is not stored in this repository.

Download it from the UCI repository and place it here as:
`dataset/covtype.csv`
